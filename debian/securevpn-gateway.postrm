#!/bin/sh
set -e

SUDOERS_FILE="/etc/sudoers.d/vpn-gateway"

case "$1" in
    purge)
        if [ -f "$SUDOERS_FILE" ]; then
            rm -f "$SUDOERS_FILE"
        fi

        # Keep /etc/openvpn/clientConfig and logs for operator safety.
        ;;
esac

exit 0
